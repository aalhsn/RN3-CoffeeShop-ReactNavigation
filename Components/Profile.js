import React, { Component } from "react";

// NativeBase Components
import { Body, List, ListItem, Label, Content, Header } from "native-base";

const Profile = props => {
  return (
    <Content>
      <Body>
        <Label style={{ color: "black" }}>Profile</Label>
      </Body>
    </Content>
  );
};

export default Profile;
